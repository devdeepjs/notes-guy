# Notes Guy Developer Notes

- This is a Swift Package Manager macOS app.
- Keep generated vault data, packaged app bundles, local planning artifacts, and raw recordings out of git.
- Validate source changes with `swift test`.
- Build the installable app with `./scripts/package-macos-app.sh`.
